"""Iteration 2: Multi-Source Research Assistant.

Workflow Agent + RAG + Tools — Pull (Query) Mode.
Medium Control · Medium Agency.

Two pipelines:
  OFFLINE — document ingestion with 3-namespace ChromaDB, MNPI gate, smart chunking
  ONLINE  — semantic cache, query analyzer, retrieval, text-to-SQL, corrective RAG,
             synthesis + divergence detection, mosaic completeness output
"""
